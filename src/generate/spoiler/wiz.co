cheerio = require \cheerio

module.exports = (html) ->
  $ = cheerio.load html

  cards = []
  $('.article-image[title]')each !->
    $el = $ @
    name = $el.attr \alt
    url = $el.attr \src
    cards.push { name, url }
  cards
